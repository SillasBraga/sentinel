import type { RiskLevel } from "@/lib/risk-engine";
const copy: Record<RiskLevel, { label: string; className: string }> = { low: { label: "Baixo", className: "bg-emerald-100 text-emerald-800" }, moderate: { label: "Moderado", className: "bg-amber-100 text-amber-900" }, high: { label: "Alto", className: "bg-orange-100 text-orange-900" }, critical: { label: "Atenção", className: "bg-rose-100 text-rose-900" } };
export function RiskIndicator({ level, factors = [] }: { level: RiskLevel; factors?: string[] }) { const state = copy[level]; return <details className="group"><summary className={`risk-pill cursor-pointer list-none rounded-full px-3 py-1.5 text-xs font-bold ${state.className}`}>Risco {state.label} · Por quê?</summary><div className="absolute right-0 z-20 mt-2 w-64 rounded-2xl border border-[var(--line)] bg-white p-4 text-sm font-normal leading-6 text-[var(--muted)] shadow-xl">{factors.length ? factors.join(" ") : "Nenhum fator de risco relevante foi identificado agora."}</div></details>; }
