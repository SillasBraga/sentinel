import type { RiskLevel } from "@/lib/risk-engine";
const copy: Record<RiskLevel, string> = { low: "Estável", moderate: "Atenção", high: "Elevada", critical: "Prioridade" };
export function RiskIndicator({ level, factors = [] }: { level: RiskLevel; factors?: string[] }) { return <details className="journey-risk group" data-level={level}><summary className="risk-pill cursor-pointer list-none rounded-full px-3 py-1.5 text-xs font-bold">Radar {copy[level]} · detalhes</summary><div className="absolute right-0 z-20 mt-2 w-64 rounded-2xl border border-[var(--line)] bg-white p-4 text-sm font-normal leading-6 text-[var(--muted)] shadow-xl">{factors.length ? factors.join(" ") : "Nenhum fator de atenção relevante foi identificado agora."}</div></details>; }
