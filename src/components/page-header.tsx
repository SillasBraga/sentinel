import Link from "next/link";
import { EyeOff } from "lucide-react";
export function PageHeader({ eyebrow, title, description, quickExit = true }: { eyebrow?: string; title: string; description?: string; quickExit?: boolean }) { return <header className="journey-page-header flex items-start justify-between gap-4"><div className="min-w-0">{eyebrow && <p className="eyebrow text-[var(--teal-deep)]">{eyebrow}</p>}<h1 className="display mt-2 text-4xl leading-tight sm:text-5xl">{title}</h1>{description && <p className="mt-3 max-w-2xl leading-7 text-[var(--muted)]">{description}</p>}</div>{quickExit && <Link href="/app/focus" className="flex shrink-0 items-center gap-2 rounded-full border border-[var(--line)] bg-white px-4 py-2.5 text-sm font-bold" aria-label="Saída rápida para tela neutra"><EyeOff size={17} /> <span className="hidden sm:inline">Saída rápida</span></Link>}</header>; }
