import { JournalEntryDialog } from "@/components/journal-entry-dialog";import { PageHeader } from "@/components/page-header";import { RecoveryItemActions } from "@/components/recovery-item-actions";import { requireUser } from "@/lib/auth";import { createClient } from "@/lib/supabase/server";
export default async function JournalPage(){const user=await requireUser();const supabase=await createClient();const{data}=await supabase.from("journal_entries").select("id,title,body,occurred_at").eq("user_id",user.id).order("occurred_at",{ascending:false}).limit(20);return <div className="mx-auto max-w-4xl p-5 py-8 sm:p-8 lg:p-12"><PageHeader eyebrow="Diário privado" title="Um lugar para organizar o que você sente." description="Seu diário não é compartilhado por padrão — nem mesmo com um parceiro de accountability."/><div className="mt-7"><JournalEntryDialog /></div><div className="mt-6 grid gap-4">{data?.map(entry=><article key={entry.id} className="rounded-[1.5rem] bg-white p-6"><div className="flex items-start justify-between gap-4"><div><time className="text-xs font-bold uppercase tracking-wider text-[var(--muted)]">{new Intl.DateTimeFormat("pt-BR",{dateStyle:"medium"}).format(new Date(entry.occurred_at))}</time>{entry.title&&<h2 className="mt-3 text-xl font-bold">{entry.title}</h2>}</div><RecoveryItemActions kind="journal" id={entry.id} title={entry.title} body={entry.body}/></div><p className="mt-3 whitespace-pre-wrap leading-7 text-[var(--muted)]">{entry.body}</p></article>)}{!data?.length&&<p className="rounded-[1.5rem] border border-dashed border-[var(--line)] p-8 text-center text-[var(--muted)]">Nenhuma nota ainda. Registre apenas quando isso fizer sentido para você.</p>}</div></div>}
