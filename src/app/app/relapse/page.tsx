import { PageHeader } from "@/components/page-header"; import { SubmitButton, TextAreaField } from "@/components/form-controls"; import { createRelapse } from "@/features/recovery/actions";
export default async function RelapsePage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;const now=new Date();now.setMinutes(now.getMinutes()-now.getTimezoneOffset());return <div className="mx-auto max-w-3xl p-5 py-8 sm:p-8 lg:p-12"><PageHeader eyebrow="Recomeço" title="Entenda o que aconteceu e continue." description="Seu streak reinicia, mas seu progresso não. Este registro existe para gerar aprendizado, não culpa."/>{error&&<p className="mt-6 rounded-2xl bg-red-50 p-4 text-red-800">{error}</p>}<form action={createRelapse} className="mt-8 grid gap-6 rounded-[1.8rem] bg-white p-6 sm:p-8"><label className="grid gap-2 text-sm font-bold">Quando aconteceu?<input required type="datetime-local" name="occurredAt" defaultValue={now.toISOString().slice(0,16)} className="min-h-13 rounded-2xl border border-[var(--line)] px-4 font-normal"/></label><TextAreaField label="O que preparou esse momento?" name="trigger"/><label className="grid gap-2 text-sm font-bold">Contexto<input name="context" placeholder="Ex.: sozinho, tarde da noite" className="min-h-13 rounded-2xl border border-[var(--line)] px-4 font-normal"/></label><TextAreaField label="O que você aprendeu?" name="learning"/><TextAreaField label="Qual é o próximo passo pequeno?" name="nextStep"/><SubmitButton>Salvar e continuar</SubmitButton></form></div>}
