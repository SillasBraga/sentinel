# Scripts

- `create-demo-user.mjs`: cria ou atualiza a conta de demonstração no Supabase local.

Execute pelo script npm documentado:

```bash
npm run db:demo
```

Scripts deste diretório devem usar ambiente local por padrão, ser idempotentes quando possível e nunca incorporar segredos.
